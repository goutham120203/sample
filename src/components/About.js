import React from "react";

const About = () => {
    const styles = {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh", // Full viewport height
        textAlign: "center",
      };
    return (
        <div style={styles}>
                <h1>about page</h1>

        </div>
    )
}

export default About